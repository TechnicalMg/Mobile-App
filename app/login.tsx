// login.tsx
import React from 'react';
import { Text, View } from 'react-native';

export default function LoginScreen() {
    return (
        <View>
            <Text>Login is disabled in this version.</Text>
        </View>
    );
}
